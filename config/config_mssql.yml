connection:
  host: "localhost"
  port: 1433
  database: "MairieMSSQL"
  user: "db_admin"
  password: "CHANGE_ME"
  driver: "ODBC Driver 18 for SQL Server"

backup:
  enabled: true
  use_native_backup_command: true
  backup_directory: "C:\\backups\\mssql"
  backup_name_prefix: "MairieMSSQL_"

maintenance:
  rebuild_indexes: true
  update_statistics: true

schemas:
  default_schema: "dbo"

data_load:
  enabled: false
